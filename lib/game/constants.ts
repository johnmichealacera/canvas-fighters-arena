export const GAME_CONFIG = {
  gravity: 0.6,
  floorPadding: 36,
  maxFallSpeed: 14,
  roundDurationMs: 60000,
  roundResetDelayMs: 3000,
  attackCooldownMs: 350,
  hitFlashDurationMs: 120,
} as const;
